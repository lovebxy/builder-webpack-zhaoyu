language: node_js

sudo: false

cache:
  apt: true
  directories:
    - node_modules

node_js: stable

install:
  - npm install -D
  - cd ./test/smoke/template
  # - npm i --legacy-peer-deps
  - npm install -D
  - cd ../../../

script:
  - npm test
